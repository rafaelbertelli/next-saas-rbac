// Enumerations used across the application

enum Role {
  ADMIN
  MEMBER
  BILLING
}

enum TokenType {
  PASSWORD_RECOVER
}

enum AccountProvider {
  GITHUB
}

enum InviteStatus {
  PENDING
  ACCEPTED
  REJECTED
}
